import type {ReactNode} from 'react';
export const metadata={title:'Museum Mood — caption studio',description:'Choose one of eight public-domain paintings, edit a caption, and download your museum reaction.'};
export default function ClassicLayout({children}:{children:ReactNode}){return children}
